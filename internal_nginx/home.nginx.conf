# map $http_upgrade $connection_upgrade {
#     default upgrade;
#     ''      close;
# }

server {
  listen 80:80;
  server_name hass.*;
  include extra/security.nginx.conf

  location / {
    proxy_pass http://home_assistant:8123/;
    include extra/proxy.nginx.conf
  }

  include extra/general.nginx.conf
}

server {
  listen 80:80;
  server_name nodered.*;

  include extra/security.nginx.conf

  location / {
    proxy_pass http://node_red:1880/;
    include extra/proxy.nginx.conf
  }

  include extra/general.nginx.conf
}

server {
  listen 80:80;
  server_name prometheus.*;

  include extra/security.nginx.conf

  location / {
    proxy_pass http://prometheus:9090/;
    include extra/proxy.nginx.conf
  }

  include extra/general.nginx.conf
}

# server {
#   listen 80;
#   server_name jenkins.*;

#   include extra/security.nginx.conf
#
#   location / {
#     proxy_pass http://jenkins:8080/;
#     include extra/proxy.nginx.conf
#   }
# }

# server {
#   listen 80;
#   server_name kibana.*;

#   include extra/security.nginx.conf
#
#   location / {
#     proxy_pass http://kibana:5601/;
#     include extra/proxy.nginx.conf
#   }
# }

server {
  listen 80 default_server;
  server_name _;

  include extra/security.nginx.conf

  location /admin {
    proxy_pass http://pihole/admin;
    include extra/proxy.nginx.conf
  }
}
